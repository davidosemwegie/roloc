# Link ring-rush with `npx convex dev --configure existing`.
CONVEX_DEPLOYMENT=
CONVEX_URL=
CONVEX_SITE_URL=
# Deployment secrets are configured via `npx convex env set`, never copied here.
# JWT_PRIVATE_KEY, JWKS, SITE_URL, RING_RUSH_CLOSED_TEST_CODE
